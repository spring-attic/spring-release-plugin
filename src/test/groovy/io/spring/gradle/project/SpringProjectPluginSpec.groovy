package io.spring.gradle.project

import spock.lang.Specification

class SpringProjectPluginSpec extends Specification {
    def 'release plugin generates releases with .RELEASE suffix'() {
        
    }
}
